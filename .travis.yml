# use the Python 3.5 language
language: python
python:
  - "3.5"

# cache to improve build speed
cache:
  directories:
    - $HOME/.pip-cache/

# command to install dependencies
install:
  - pip install --upgrade pip
  - pip install -r requirements.txt --cache-dir $HOME/.pip-cache

# set GATORGRADER_HOME environment variable
before_script:
  - export GATORGRADER_HOME="/home/travis/build/gkapfham/gatorgrader"

# perform testing:
# --> run the internal test suite with pytest
# --> demonstrate the use of gatorgrader.py on itself
script:
  - pytest tests
  - python3 gatorgrader.py --directories tests tests --checkfiles conftest.py test_gatorgrader_submit_files.py
